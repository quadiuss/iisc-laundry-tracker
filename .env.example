# These are only used by the Netlify Function (server-side).
# Set the real values in the Netlify Dashboard -> Site settings -> Environment variables
# NEVER commit real values or put the service role key in frontend code.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-secret-service-role-key
